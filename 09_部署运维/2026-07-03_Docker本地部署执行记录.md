# 2026-07-03 Docker 本地部署执行记录

## 已完成

- 新增 `docker-compose.test.yml`，用于本地 Docker 测试环境。
- 新增 `.env.local` 和 `.env.local.example`，本机测试不覆盖生产 `.env`。
- 新增 `scripts/local-docker.ps1`，统一本地 Docker 启停、日志、健康检查和清空测试数据。
- `scripts/local-docker.ps1` 已兼容 Docker Compose v2 和独立 `docker-compose.exe`。
- 新增 `scripts/healthcheck.ps1` 和 `healthcheck.sh`。
- 新增 `scripts/enable-windows-docker-features.ps1`，用于管理员 PowerShell 启用本机 Docker/WSL 必要功能。
- 新增 `docker-compose.prod.yml`，保留生产 Compose 标准入口。
- 新增 `rollback.sh`，统一回滚入口。

## 已验证

| 检查项 | 结果 |
|---|---|
| `docker-compose --project-name delivery-platform-local --env-file .env.local -f docker-compose.test.yml config` | 通过 |
| `docker compose --env-file .env.example -f docker-compose.prod.yml config -q` | 通过 |
| `scripts/local-docker.ps1` daemon 检查 | 可给出明确错误提示 |
| 发布包重新生成 | 通过 |
| 发布包 SHA256 | 通过 |

## 本轮新增验证内容

- 后端类型检查：通过。
- 后端测试：25 个测试套件，105 个用例通过。
- 后端构建：通过。
- 前端类型检查：通过。
- 前端测试：14 个测试文件，59 个用例通过。
- 前端构建：通过。
- 前后端 `pnpm audit --prod`：无已知漏洞。
- 知识库样例文件已生成并纳入种子脚本，Docker 初始化后会写入 MinIO。

## 当前阻塞

本机 Docker daemon 未运行，`docker info` 返回无法连接 `npipe:////./pipe/docker_engine`。

进一步直接启动 Scoop `dockerd.exe` 的错误为：

```text
system requirements not met: failed to load vmcompute.dll, ensure that the Containers feature is installed
```

当前 Codex 进程不是管理员，无法执行 `Get-WindowsOptionalFeature` 或启用 Windows Containers/Hyper-V/WSL 功能，因此本轮不能在这台机器上真正启动容器。

## 待管理员执行

1. 使用管理员 PowerShell 执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\enable-windows-docker-features.ps1
```

2. 重启电脑。
3. 启动 Docker Desktop 或确保 Docker daemon 正常运行。
4. 回到项目目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\local-docker.ps1 up -Build
```

## 预期访问地址

- 平台：`http://delivery-platform.localhost:18080`
- 后端健康检查：`http://delivery-platform.localhost:18080/api/v1/health`
- 后端直连调试：`http://127.0.0.1:13000/api/v1/health`
