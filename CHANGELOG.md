# 更新记录

## 2026-08-24（项目概览编辑与 Arco 统一）

- 项目详情查看态新增受 `project:update` 控制的“编辑项目”入口，沿用统一编辑弹窗保存当前阶段、项目进度、验收与基础信息；款项计划继续由 `payment:operate` 独立控制。
- 角色权限目录统一为“项目概览 / 款项计划”，明确展示“编辑项目”和“编辑款项计划”，并增加真实权限目录分配的浏览器验收契约。
- 款项计划表格补齐单元格竖向边框；项目概览的统计、按钮、图标、项目链接、标签和进度条统一使用 Arco Design Vue 原生组件。

## 2026-08-18（角色权限管理）

- 系统设置新增仅超级管理员可见的“角色权限”入口，提供角色新建、编辑、删除及按模块、页面、功能和查看/操作/传输/删除动作分组的树状权限矩阵，支持整表、模块、页面和列级批量勾选或取消。
- 权限目录新增持久化展示元数据；新增页面或功能时，权限码与目录元数据会自动形成权限矩阵行列。用户中心改用受控可分配角色接口，角色变更继续递增权限版本，使旧会话立即失效。
- `SUPER_ADMIN` 升级为受保护角色：固定启用并拥有全部权限，禁止删除或修改权限集合；非超级管理员不能分配该角色，系统拒绝移除、禁用或删除最后一个可用超级管理员账号。
- 角色权限管理 API 增加服务端角色终检和拒绝审计，前端菜单、路由和操作入口同步执行角色约束；Prisma migration 总数更新为 48，并同步发布门禁与架构、测试文档。

## 2026-08-10（飞书内嵌侧栏布局）

- 应用壳按固定尺寸约束侧栏：展开态 `180px`、收起态 `48px`，菜单图标固定为 `18px`，菜单行、文字与折叠入口禁止 flex 收缩和异常换行。
- 主内容区改为占用侧栏之外的剩余宽度，并补齐根节点高度、动态视口、最小宽度和溢出约束，避免飞书 WebView、同源 iframe、缩放及窄桌面窗口挤压导航栏。
- 窄桌面嵌入环境不再被仅按宽度误判为手机；抽屉模式仅用于窄屏触控设备。新增普通浏览器、飞书 User-Agent、同源 iframe、多尺寸及展开/收起的自动化布局契约。

## 2026-08-04（飞书身份、分层权限与测试环境新基线）

- 飞书集成扩展为完整组织通讯录同步：同步根部门、全部子部门、用户多部门关系、头像及 `user_id/open_id/union_id`，按用户独立事务统计新增、更新、停用、跳过和失败，并保护超级管理员与人工绑定账号。
- 新增飞书 OAuth 扫码登录：服务端交换授权码，一次性 state 和登录 ticket 只以哈希键短期保存于 Redis，系统继续签发短期 Access Token 和 HttpOnly Refresh Cookie；登录页保留管理员密码入口。
- 权限解析增加用户显式覆盖、直接角色、直属组织和逐级上级组织，近层优先、同层禁止优先；权限解析改为必需依赖，用户或组织权限变更会递增权限版本并在同一事务写入带 traceId 的审计。
- Refresh Token 轮换后会立即把新会话同步回 Pinia；若用户已停用，则撤销刚创建的子会话，避免前端误判登出或遗留可用刷新凭据。
- 正式 seed 默认只创建唯一 `admin`，示例用户和项目必须显式启用 `SEED_INCLUDE_DEMO_DATA=true`；可销毁测试服务器支持受控删除精确解析出的 MySQL、Redis、MinIO 三个卷，随后迁移、基础 seed、全表计数和外键孤儿校验。
- 迁移总数增加到 46；同步更新 Release Manifest、服务器预检、运行时模板、部署教程、架构与安全文档，并补齐轻量视觉验收中管理员登录按钮的稳定选择器。

## 2026-08-02（轻量测试与不可变发布架构）

- 本地默认质量检查改为 PowerShell 7 下的 Node 进程内流程，不启动 WSL、Docker、MySQL、Redis 或 MinIO；新增项目台账、项目档案、档案模板、项目弹窗和标准库的设计/实现/差异视觉报告。
- CI 在质量门禁后只构建一次：后端运行镜像和迁移镜像固定为 GHCR digest，前端静态包与 Release Manifest 发布为 OCI Artifact；真实 MySQL、Redis、MinIO、API 和浏览器验收全部使用同一组正式发布物。
- 测试环境自动部署，生产环境只允许从测试验证凭据推广同一完整 SHA，并通过 `production` GitHub Environment 人工审批；旧部署工作流仅保留手动迁移兜底。
- 测试和生产服务器拆分为宿主 Nginx、应用 Compose 和内部数据 Compose；前端采用符号链接原子切换，数据端口只绑定 localhost，所有文件继续统一使用 500 MiB 上限。
- 新增发布前停写、MySQL/MinIO 成对备份、迁移门禁、内外网 Release ID 校验、失败标记和显式成对恢复；恢复时校验 checksum、源 Release、Worker 稳定性和服务器身份，不自动删除备份或 Docker 卷。
- 新增测试/生产账号、SSH、sudo、目录、内外网地址、Nginx、GitHub Variables/Secrets、首发接管、日常推广和回滚的完整中文教程。
- 发布镜像按后端 Git 树内容 SHA-256 复用，runtime 与 migrator 使用独立构建缓存，首次构建固定 `SOURCE_DATE_EPOCH=0`；仅文档或部署脚本变化时不再重建后端，同时保留不可变 Release 与默认来源证明。
- Release `79e41eb29d40` 的同 SHA 复跑已验证内容复用：runtime 与 migrator 构建均跳过、两份 Manifest 的镜像 digest 完全一致，测试服务器拉取不足 0.2 秒、切换 53 秒、整条流水线 9 分 09 秒；首次内容冷拉取仍受 GHCR 链路质量影响，并由在线预拉取、20 分钟超时续传和停机前失败保护控制风险。

## 2026-07-28（标准库 Figma 精确对齐）

- 以 Figma 节点 `70:322` 为标准库唯一视觉基准，重构三项统计、关键词工具栏、交付阶段/管理领域分类工作区和五列固定宽度表格；统一 88px 指标区、270/937px 双栏、44px 行高、`365/90/130/170/182px` 列宽、纵向滚动和无分页器布局。
- 标准类型、交付阶段、管理领域、业务类型、状态、启停、当前版本、生效日期与使用国家统一接入字段配置；重命名、排序、默认值和启停自动传播，历史停用值继续按稳定编码展示。
- 标准主数据、国家关联和文件版本使用稳定编码或主键关联；真实后端支持筛选、排序、`page/pageSize` 分页、分类计数、版本历史、审核发布、当前版本、生效、启停、下载、归档和权限终检。
- 新增第 43 个 Prisma migration：回填新版标准字段和国家关联，停用旧 `STANDARD_CATEGORY` 并删除 `Standard.category`，不保留运行时双读或双写。
- 新增标准库说明、真实 API 生命周期测试和 Figma 几何浏览器契约；最终门禁与部署结果见 [测试验收](docs/testing.md)。

## 2026-07-28（项目概览 Figma 精确对齐）

- 以 Figma 节点 `23:15329` 为项目概览主内容唯一视觉基准，重构五项只读统计、查询工具栏和 13 列固定宽度表格；统一 1234×784 内容尺寸、图标、标签、进度条、固定列阴影、滚动条、44px 行高及 1280/1440/1920 响应式零溢出。
- 删除设计中不存在的统计卡筛选、刷新按钮、额外统计字段及旧刷新图片；查询改为点击或回车后提交，项目经理排序由真实后端支持，通用项目排序能力保持兼容。
- 国家、币种、客户/合同类型和项目阶段继续由字段配置同源提供；列表保留历史停用值展示，详情与表单只使用当前配置，真实 API E2E 验证改名、排序、启停和权限边界。
- 更新项目概览前后端契约、国际化、架构与测试文档；前端 43/215、后端 74/528 测试全部通过，真实 API 5/5、依赖冒烟 2/2、Chromium UI 15/15 通过。

## 2026-07-27（Figma 与字段配置闭环）

- 只读取指定 Figma 文件中的 App Shell、项目概览、项目档案、档案模板、标准库、知识库、审批规则和字段配置 8 个节点，按实际节点完成页面骨架、指标、筛选、表格、工具栏和 10 个字段页签重构。
- 国家、币种、项目/客户/合同/产品类型、关键词、阶段、状态及标准/知识分类统一接入 `DictionaryCategory/DictionaryItem`；新增业务只读 `/field-options`、管理 `/field-config`、同源后端写入校验和字段配置修订。
- 新增 6 个顺序 migration，将迁移总数提升为 41；拆分字段配置权限，增加项目客户类型，关联知识分类，导入国家/币种选项并退役临时档案项权限。
- 删除旧项目配置/知识分类接口、项目归档列表 UI、临时档案项创建、币种创建、旧本地模拟知识端点及失效设计交接文档。
- 测试服务器随机数据改为读取当前启用的字段配置；新鲜数据库的示例项目补齐项目类型，并校正项目经理权限、归档入口、配置标签和表格宽度的真实浏览器验收契约。
- 新增 Figma 页面清单、字段配置说明、删除报告和平台目标架构，并同步 README、前后端规范、开发、部署、安全、路由、权限、API 与测试说明。
- 前端 43/213、后端 74/526 测试全部通过；lint、类型检查、生产构建、Compose、架构/Prisma/权限/文档事实门禁、41 个迁移、真实 API E2E 和 8 个目标页面浏览器验收通过。

## 2026-07-26（侧栏导航联动）

- 修复 CSS Mask 将侧栏 SVG 图标渲染为空白或色块的问题；恢复使用真实 SVG 图片保留透明轮廓，并通过当前路由分别控制激活与默认颜色。
- 修复项目管理图标 SVG 自带蓝色导致数据看板选中时出现错误高亮的问题；一级图标改为按路由语义显式映射，并由当前激活组统一控制颜色。
- 侧栏的一级标题、图标、展开组和二级选中项统一使用同一导航结果；系统设置组补齐语义图标，隐藏创建/详情路由使用 `meta.activeMenu` 保持正确菜单归属。
- 新增四个一级分组、15 个可见二级入口和 7 个创建/详情深链的导航契约与真实环境浏览器验收。

## 2026-07-25（基础架构）

- 基于 Figma 项目说明、当前源码和正式文档建立《交付管理平台基础架构模型 v0.1》，明确平台定位、当前代码结构、`app / design-system / modules / shared / services` 目标分层及渐进迁移原则。
- 校正前端实施规范中面包屑与当前 App Shell 实现的冲突，并同步当前架构、开发规范、文档入口和维护责任。
- 删除未采纳且与当前正式架构存在分叉的前端重构评审稿；版本历史统一由本文件维护，不再保留重复的 `docs/release.md`。

## 2026-07-25

- 按左侧二级目录重新整理“系统设置”，统一用户中心、币种与汇率、通知规则、审批规则、字段配置、集成配置和系统配置的菜单、路由、页面标题及业务归属。
- 补齐用户中心入口，集中提供用户列表、基本资料、账号状态、角色关联以及新增、编辑、启停、重置密码和删除操作。
- 删除无菜单归属的访问日志页面、读取接口、权限和前端死代码；保留后端操作审计写入能力，继续记录敏感操作。
- 新增权限清理迁移，并同步更新迁移数量校验、架构说明、业务流程和测试文档。
- 完成前后端 lint、类型检查、单元测试、构建、Docker Compose 校验，以及基于 NestJS、MySQL、Redis 和 MinIO 的真实浏览器验收。

## 2026-07-17

- 项目概览的查看入口使用 800px 居中扁平化详情弹窗，顶栏统一承载编辑、进度、归档和关闭操作；正文按基本信息、阶段合同、项目团队、风险档案四个分区展示，并保留后端操作权限、敏感字段裁剪与列表 query。

## 2026-07-16

- 项目类型、合同类型、产品和关键词统一由数据库启用字典提供；新增项目配置聚合接口与服务端写入终检，项目列表、详情、表单和档案模板共用同一配置来源。
- 新增文档事实门禁、开发环境预检和前端构建体积预算并接入本地质量脚本与 CI；补齐审计失败路径与项目页面国际化防回退契约。
- 真实发布验收新增 32 个迁移及源码校验和核对、二次 seed 全库表计数比较，以及私有 MinIO 文件逐字节回读、缩略图 Worker 和 Outbox 事件收敛门禁。
- 按当前源码全面校正文档：同步项目分类、负责人字段、统一进度与验收命令、归档列表、29 个 Prisma migration、前后端测试规模和直接依赖清单。
- 前端页面架构、业务流程及前后端实施规范统一直接描述当前实现；文档索引、产品规则、总体架构和测试验收使用同一事实口径。
- 当前自动化验证通过：前后端 ESLint、类型检查、测试与生产构建，前端 41 个测试文件 / 190 个用例，后端 71 个测试套件 / 507 个用例，Prisma Client 生成与 schema 校验。
- Docker Engine 真实验收通过：32 个 migration 与源码 SHA-256 完全一致，三组 migrator 的 dry-run/apply/只读 verify、二次幂等 seed、真实 API 及字段配置浏览器全流程均成功。
- 项目档案树公开不可变上传策略，文件处理状态公开 `outputAssetId`；浏览器验收可根据现有文件自动选择首传或新版本模式，重复执行不会依赖空档案项。

## 2026-07-15

- 重构项目概览、详情、创建/编辑抽屉和归档列表：新增 mine/all 数据范围切换、项目分类与关键词、统一进度命令和后端计算的操作权限，正常列表移除操作列及阶段行内编辑。
- 创建与编辑统一复用 `ProjectDrawer`，新增响应式表单基础组件；项目团队移除采购、财务负责人，项目类型、合同类型、产品和关键词接入平台字典并统一标签颜色。
- Prisma 新增合同类型、产品、关键词、归档操作人，统一电气/软件负责人字段并删除旧负责人列；seed 覆盖多分类、阶段、进度、验收与归档数据且保持二次运行不覆盖既有项目。
- 项目阶段和验收写入口收敛为 `PATCH /projects/:id/progress`，新增归档列表与永久删除专用接口；前后端完整 lint、类型检查、测试、构建、Prisma 和 Compose 静态门禁通过。

## 2026-07-14

- 修复同一 commit 重试部署时覆盖运行中 release tag、导致旧容器 Image ID 无法进入成对备份的问题；已成功发布的同版本后端和前端镜像现在必须通过 OCI release label 校验并直接复用，仅重建临时迁移镜像。
- 补充 GitHub Environment、仓库角色、部署账号、SSH、Docker 和网络的最小权限接入边界，明确服务器不保存 GitHub Token，生产与测试必须使用隔离凭据。

## 2026-07-13

- GitHub 部署成功后新增受保护的服务器镜像清理：保留全部容器、当前/上一成功发布及 checksummed v3 备份引用的 Image ID，只以非强制方式删除其余镜像；清理前后复核磁盘、API、Worker、前端和发布标签，绝不清理 Docker volume。

- 按前后端重构实施规范完成工作台、项目、两级档案、档案模版、统一文件与审核、标准、知识、工具、组织权限和系统设置的运行时切换。
- Prisma migration 增至 28 个；新增 UI 翻译表无损归档和标准/知识文件化准备迁移，目标内容、档案/项目与集成 Secret migrator 统一接入 dry-run/apply/只读严格门禁；旧业务表仅作为只读迁移源保留。
- 文件统一为 LogicalFile、FileAsset 和不可变 FileVersion，审核统一为 ReviewTask/Step/Assignee/ActionEvent，支持单人、全员、ANY_N、串行与并行审批。
- 前端统一使用 Arco Design Vue、业务组件、TanStack Query、权限路由和单一只读文件预览入口，删除旧页面、旧 API、兼容组件和重复状态流程。
- API、File Worker 与 Outbox Worker 独立运行；通知投递、文件处理、敏感操作审计、Refresh Token 轮换、数据范围和敏感字段裁剪完成加固。
- 合并后 lint、类型检查、Prisma 校验和生产构建通过；前端 40/175、后端 67/464 全部通过。此前隔离真实环境的 API 2/2、依赖冒烟 2/2、Chrome 关键流程 2/2 作为基线保留，最新 28 migration 的真实依赖验收以同提交 GitHub integration/deploy 为准。
- 修复 GitHub 自动部署在解压发布包后污染服务器 Git 工作区、又因脏工作区门禁无法切换提交的问题；改为校验并导入固定 SHA 的 Git bundle，且在切换前同时保护受跟踪和未跟踪源码。
- Dockerfile 改用 Docker/BuildKit 内置解析器，移除对 `docker/dockerfile:1.7` 外部 frontend 镜像的强制构建时依赖，同时保留内置 BuildKit 支持的依赖缓存挂载，兼容受限镜像源的部署服务器。
- API、File Worker 与 Outbox Worker 统一使用同一 release 后端镜像；部署在 migration 前按 Worker→API 顺序静默，切换后按 API→Worker→前端顺序启动并校验四个容器的 release 标签，避免新旧进程混跑。
- 修复生产 `.env` 缺少集成加密密钥时 MySQL 就绪等待丢失候选密钥的问题；密文检查与密钥持久化改为在 API/Worker 停写后、备份和 migration 前执行，并为自动部署、人工备份和代码回滚补齐顺序与恢复契约测试。
- 标准库删除 ONLINE/结构化正文双轨并强制有效文件版本；知识库强制 FILE/MARKDOWN/LINK 三选一、显式支持文件集合和版本化修改，公开响应统一裁剪 legacy 与对象存储元数据。
- MinIO 备份改为健康容器与唯一命名卷双门禁、临时归档校验后原子发布；不再允许空归档或数据库单边备份。表计数门禁显式核对 UI 翻译归档前后行数，其他表消失仍会阻断部署。

## 2026-07-08

- 将项目文档统一整理为 GitHub 风格结构，文档入口集中到 `docs/`。
- 删除历史编号阶段目录、旧发布包脚本、旧发布包测试和临时产物。
- 新增贡献规范、安全说明、开源依赖说明和开源准备清单。
- 新增直接依赖许可证清单、中文 Issue 模板和中文合并请求模板。
- 修正文档和包描述中的乱码内容。
- 将项目阶段 `04_construction` 统一为“施工与调试 / Construction & Commissioning”。
- 前端测试、前端构建、后端类型检查和 Git 空白检查已通过。

## 2026-07-03

- 按公司标准补齐项目交付目录、本地 Docker 测试部署、健康检查和回滚入口。
- 新增 `docker-compose.test.yml`，用于本机 Docker 测试环境。
- 新增 `.env.local.example` 和本地 `.env.local`，本地访问域名为 `delivery-platform.localhost`。
- 生产数据库同步保留 `prisma migrate deploy`，禁止发布链路使用 `db push --accept-data-loss`。
- 修复前后端依赖审计漏洞，生产依赖审计已无已知漏洞。
