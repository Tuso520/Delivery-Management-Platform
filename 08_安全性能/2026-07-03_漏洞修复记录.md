# 2026-07-03 漏洞修复记录

## 修复范围

- 后端依赖升级到 NestJS 11 安全线。
- 后端 `bcrypt`、`helmet`、`multer`、`uuid` 升级到安全版本。
- 后端通过 `pnpm.overrides` 固定传递依赖 `multer@2.2.0`、`qs@6.15.3`。
- 前端升级 `axios@1.18.1`、`vue-i18n@9.14.5`、`element-plus@2.11.1`。

## 验证结果

| 检查项 | 结果 |
|---|---|
| 后端 `pnpm audit --prod` | 无已知漏洞 |
| 前端 `pnpm audit --prod` | 无已知漏洞 |
| 后端类型检查 | 通过 |
| 后端测试 | 25 个测试套件、103 个测试通过 |
| 后端构建 | 通过 |
| 前端类型检查 | 通过 |
| 前端测试 | 14 个测试文件、59 个测试通过 |
| 前端构建 | 通过 |

## 备注

- `uuid@14` 为纯 ESM，会破坏当前 Jest 29 CJS 测试链路，因此使用满足安全要求且兼容现有测试配置的 `uuid@11.1.1`。
- 前端构建仍存在 Sass legacy JS API 和大 chunk 警告，不阻断本次本地测试部署整改，后续应做分包优化。
