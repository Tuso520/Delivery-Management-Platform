# 标准流程复盘

## 做得正确的部分

- 将文档入口统一到标准目录，减少旧 `docs/` 与新流程并存造成的混乱。
- 将 UI 组件库口径统一为 Arco Design Vue，避免旧组件库和 Arco 混用。
- 将知识库预览作为专项验收，覆盖 doc、xlsx、ppt、pdf 和图片。
- 将 Git 推送部署和服务器数据保护纳入运维标准。

## 暴露的问题

- 旧文档中存在过时组件库和部署方式描述。
- 部分页面存在乱码文案，需要在提交前加入扫描。
- 本地模拟服务和真实后端接口需要持续保持字段一致。

## 后续改进

- 增加 Playwright 页面级自动化，覆盖登录、知识库、文件预览、系统设置。
- 增加真实 MinIO 文件上传和预览集成测试。
- 增加权限矩阵测试，覆盖角色、菜单、按钮和 API。
- 在 CI 中加入乱码扫描和敏感信息扫描。
